[package]
name = "hft-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam-channel = "0.5"
rand = { version = "0.8", features = ["std", "small_rng"] }

[dev-dependencies]
criterion = "0.5"
